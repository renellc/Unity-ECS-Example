using UnityEngine;

public class Bullet : MonoBehaviour
{
    /**
     * How long the bullet lives in the game world before being
     * destroyed. The bullet is destroyed when this is equal to 0.
     */
    public float BulletLiveTime;
}
